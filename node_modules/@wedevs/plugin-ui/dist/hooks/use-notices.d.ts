import { Notice, NoticeAction } from '@/components/wordpress/AdminNotice';
interface NoticesOptions {
    interval?: number;
    autoSlide?: boolean;
    notices?: Notice[];
    noticesUrl?: string;
    noticesUrlArgs?: Record<string, string>;
    actionUrl?: string;
}
export declare const useNotices: ({ interval, autoSlide, notices: staticNotices, noticesUrl, noticesUrlArgs, actionUrl }?: NoticesOptions) => {
    notices: Notice[];
    error: string;
    isLoading: boolean;
    currentNotice: number;
    nextNotice: () => void;
    prevNotice: () => void;
    pauseAutoSlide: () => void;
    resumeAutoSlide: () => void;
    closeNotice: (notice: Notice, index: number) => Promise<void>;
    executeAction: (action: NoticeAction, noticeIndex: number) => Promise<void>;
    actionLoading: {
        [key: number]: boolean;
    };
};
export {};
//# sourceMappingURL=use-notices.d.ts.map