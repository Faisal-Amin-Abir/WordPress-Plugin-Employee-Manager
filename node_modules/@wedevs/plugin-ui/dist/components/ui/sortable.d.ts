import * as React from "react";
import { type DndContextProps, DragOverlay, type DragEndEvent, type UniqueIdentifier } from "@dnd-kit/core";
import { type SortingStrategy } from "@dnd-kit/sortable";
import { useRender } from "@base-ui/react/use-render";
interface SortableProps<TData extends {
    id: UniqueIdentifier;
}> extends DndContextProps {
    items: TData[];
    onValueChange?: (items: TData[]) => void;
    onMove?: (event: DragEndEvent) => void;
    strategy?: SortingStrategy;
    children: React.ReactNode;
    overlay?: React.ReactNode;
    /**
     * Whether to wrap the component in a DndContext.
     * Set to false if you are providing your own DndContext at a higher level (e.g. for multiple lists).
     * @default true
     */
    container?: boolean;
}
declare const Sortable: <TData extends {
    id: UniqueIdentifier;
}>({ items, onValueChange, onMove, strategy, children, overlay, sensors: propSensors, collisionDetection, container, ...props }: SortableProps<TData>) => import("react/jsx-runtime").JSX.Element;
interface SortableItemProps extends Omit<useRender.ComponentProps<"div">, "id"> {
    id: UniqueIdentifier;
    disabled?: boolean;
    /**
     * Whether to use a drag handle.
     * If true, listeners will be attached to the SortableDragHandle component.
     * If false, listeners will be attached to the SortableItem itself.
     */
    useHandle?: boolean;
}
declare const SortableItem: ({ id, children, render, className, disabled, useHandle, ...props }: SortableItemProps) => import("react/jsx-runtime").JSX.Element;
interface SortableDragHandleProps extends useRender.ComponentProps<"button"> {
}
declare const SortableDragHandle: React.ForwardRefExoticComponent<Omit<SortableDragHandleProps, "ref"> & React.RefAttributes<HTMLButtonElement>>;
interface SortableProviderProps extends DndContextProps {
    children: React.ReactNode;
}
declare const SortableProvider: ({ children, sensors: propSensors, ...props }: SortableProviderProps) => import("react/jsx-runtime").JSX.Element;
export { Sortable, SortableItem, SortableDragHandle, SortableProvider, DragOverlay, type SortableProps, type SortableItemProps, type SortableDragHandleProps, type SortableProviderProps, };
//# sourceMappingURL=sortable.d.ts.map