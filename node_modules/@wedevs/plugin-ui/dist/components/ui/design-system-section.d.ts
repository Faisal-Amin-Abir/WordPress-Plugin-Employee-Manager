import { type HTMLAttributes, type ReactNode } from "react";
export interface DesignSystemSectionProps extends Omit<HTMLAttributes<HTMLDivElement>, "title"> {
    /**
     * Section title (e.g. "Buttons", "Inputs").
     */
    title?: ReactNode;
    /**
     * Optional description below the title.
     */
    description?: ReactNode;
    /**
     * Section content (component previews, swatches, etc.).
     */
    children?: ReactNode;
    className?: string;
}
/**
 * Section container for design system documentation.
 * Maps to a Figma frame: title + optional description + content area.
 *
 * @example
 * ```tsx
 * <DesignSystemSection title="Buttons" description="Primary actions">
 *   <ComponentPreview label="Default">
 *     <Button>Submit</Button>
 *   </ComponentPreview>
 * </DesignSystemSection>
 * ```
 */
export declare const DesignSystemSection: import("react").ForwardRefExoticComponent<DesignSystemSectionProps & import("react").RefAttributes<HTMLDivElement>>;
export interface ComponentPreviewProps extends HTMLAttributes<HTMLDivElement> {
    /**
     * Label for this preview (e.g. "Primary", "Default").
     */
    label?: ReactNode;
    /**
     * Preview content (the component instance).
     */
    children?: ReactNode;
    className?: string;
}
/**
 * Single row in a design system section: optional label + content.
 *
 * @example
 * ```tsx
 * <ComponentPreview label="Primary">
 *   <Button>Click me</Button>
 * </ComponentPreview>
 * ```
 */
export declare const ComponentPreview: import("react").ForwardRefExoticComponent<ComponentPreviewProps & import("react").RefAttributes<HTMLDivElement>>;
export default DesignSystemSection;
//# sourceMappingURL=design-system-section.d.ts.map